---
- name: set yum-cron config template
  when:
  - aspects_yum_cron_enabled
  - ansible_os_family == "RedHat"
  template:
    src: "etc-sysconfig-yum-cron.j2"
    dest: "/etc/sysconfig/yum-cron"
  notify:
    - restart yum-cron
  tags:
    - aspects_yum_cron
